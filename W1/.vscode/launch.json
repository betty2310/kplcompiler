{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "program": "${fileDirname}/main",
            "type": "lldb",
            "request": "launch",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "gcc make"
        }
    ]
}